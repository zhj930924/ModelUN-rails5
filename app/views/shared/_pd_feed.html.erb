<% if @pd_feed.any? %>
  
  <ol class="directives">
    <%= render partial: "directives/personal_directives", collection: @pd_feed, as: 'directive' %>
  </ol>
  
  <%= will_paginate @pd_feed, :param_name => 'pd_page' %>
<% end %>