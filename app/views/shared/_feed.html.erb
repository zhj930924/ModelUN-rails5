<% if @feed_items.any? %>
  <ol class="directives">
    <%= render partial: "directives/notes", collection: @feed_items, as: 'directive' %>
  </ol>
  <%= will_paginate @feed_items, :param_name => 'page' %>
<% end %>